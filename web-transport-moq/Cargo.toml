[package]
name = "web-transport-moq"
description = "WebTransport library for moq-noq, the MoQ fork of Noq"
authors = ["Luke Curley"]
version.workspace = true
repository.workspace = true
license.workspace = true
rust-version.workspace = true
edition = "2021"
workspace = ".."

keywords = ["quic", "http3", "webtransport"]
categories = ["network-programming", "web-programming"]

[package.metadata.docs.rs]
all-features = true

[features]
default = ["aws-lc-rs"]
aws-lc-rs = ["noq/aws-lc-rs", "rustls/aws-lc-rs"]
ring = ["noq/ring", "rustls/ring"]
# Unlocks `noq::TransportConfig::qlog_*`, which this crate re-exports but cannot
# enable on a caller's behalf.
qlog = ["noq/qlog"]

[dependencies]
bytes = "1"
futures = "0.3"
http = "1"
kio = "0.5.4"
noq = { package = "moq-noq", path = "../noq", version = "1.3.1", default-features = false, features = [
    "tracing-log",
    "platform-verifier",
    "runtime-tokio",
    "rustls",
    "bloom",
] }
rustls = { version = "0.23", default-features = false, features = [
    "logging",
    "std",
] }
rustls-native-certs = "0.8"
thiserror = "2"
tokio = { version = "1", default-features = false, features = [
    "io-util",
    "macros",
    "time",
] }
tracing = "0.1"
url = "2"
web-transport-proto = "0.6"
web-transport-trait = "0.5"

[dev-dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
rcgen = "0.14"
rustls-pemfile = "2"
tokio = { version = "1", features = ["full"] }
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
